appId: "glab.gdm-login-background"
productName: GDM-login-background-test
artifactName: ${productName}-${version}.${ext}

# Package app code into a asar archive. Set to false to debug issues.
asar: true